import { types } from "joi";
import request from "supertest";
import { app } from "../src";
import "@types/jest";
describe("second test file", () => {
  it("another math test", () => {
    let z = "ahmed";
    expect(3 + 4).toBe(7);
  });
});
